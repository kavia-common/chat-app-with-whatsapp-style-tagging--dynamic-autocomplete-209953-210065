# FrontendWebApplication environment example

# PUBLIC INTERFACE
# Base URL for the Backend API Service (should match backend port)
NEXT_PUBLIC_API_BASE=http://localhost:3001

# PUBLIC INTERFACE
# External URL where this frontend is served (used for redirects or links)
NEXT_PUBLIC_FRONTEND_URL=http://localhost:3000

# PUBLIC INTERFACE
# Mirror of environment for client logic toggles
NEXT_PUBLIC_NODE_ENV=development

# PUBLIC INTERFACE
# Logging verbosity for client code (debug|info|warn|error)
NEXT_PUBLIC_LOG_LEVEL=info

# PUBLIC INTERFACE
# Comma-separated flags to toggle features, e.g. "new-suggestions,perf-metrics"
NEXT_PUBLIC_FEATURE_FLAGS=
